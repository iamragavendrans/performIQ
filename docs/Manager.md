\#Manager



1. Dashboard

   * unwanted cards (role/ai powered/empathy)
   * unwanted welcome message
   * Instead we can use team-specific insightful information there
2. My Team

   * cards are useful here (total number of team members \& active goals)
   * list of team members (we can keep goal id, so that we can make this looks like a heatmap or some kind of table, where they are good(green)/needs improvement(yellow)/off track(red)
   * Goal ID vs Goal Description on Toggle, collapsed by default
   * Team members can be added/modified/deleted - which should reflect in the common database when creating an user from admin. instead we can make it link/unlink while admin manages the creation and deletion.
   * the manager can link the employee to his project/unlink but needs approval from admin
   * can assign/reassign goals from existing set of goals assigned by default.
3. Goal Management

   * Any Team/Client Specific Goals (like certification/course/pre-req)
   * It should list all the unique goals available under the manager's employee. if employee x has 3 goal, and y has 2 goals and both of them share 2 common goals, the total goal should appear here is 3.
   * Can add or remove custom goals
4. Team Report

   * It can use filters, based on specific manager, duration or specific employee.
   * It should give very detailed reporting insights
   * Here, the report should be team-focused. not very specific to one employee.can be drilled down further to see employee specific.

